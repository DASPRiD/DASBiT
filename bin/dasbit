#!/usr/bin/env python

try:
    import dasbit
except:
    import sys, os
    sys.path.append(
        os.path.realpath(os.path.dirname(os.path.abspath(sys.argv[0])) + '/..')
    )

from dasbit.core import Frontend

frontend = Frontend()
frontend.run()
